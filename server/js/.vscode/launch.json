{
    "configurations": [
        
    
        {
            "type": "node-terminal",
            "name": "Run Script: start:dev",
            "request": "launch",
            "command": "npm run start:dev",
            "cwd": "${workspaceFolder}",
            "runtimeVersion": "14.18.1"
        }
    ]
}